{
  "name": "s3-event-store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1079.0",
    "@types/node": "^24.0.0",
    "aws4fetch": "^1.0.20",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
